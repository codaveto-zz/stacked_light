name: stacked
description: An architecture and widgets for an MVVM inspired architecture in
  Flutter. It provides common functionalities required to build a large
  application in a understandable manner. Forked off https://github.com/FilledStacks/stacked;

version: 1.0.0
homepage: https://github.com/codaveto/stacked

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  get_it: ^7.2.0
  provider: ^6.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
